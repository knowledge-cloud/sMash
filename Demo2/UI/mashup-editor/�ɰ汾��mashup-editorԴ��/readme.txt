block.swf是之前版本的我们保存在服务器上的mashup-editor。

由于我的硬盘出了点问题，以前的mashup-editor源文件丢了，但是编译好的swf在服务器上还有备份，就是这个block.swf。

因此可以利用SWF Decompiler来反编译block.swf以得到源代码文件，即fla项目。我已经反编译好了，放在该目录下了。

由于SWF反编译器的功能有限，反编译出来的项目文件存在一定的缺陷，Actionscript代码中部分变量名称和源代码中的名称不一致，并且源代码中的元件名称也被改变，比如sprite 273中，_loc3和_loc6在源代码中实际上叫做method和methodsList。这只影响代码的可阅读性，并不影响代码的正确性。

但是存在一个问题，即SWF Decompiler反编译出的项目文件版本选择上总是存在问题，因此反编译出的fla项目不能编译通过，报错也基本上就是说“该版本的flash播放器不能加载XXXX函数“，这个问题我也暂时没有解决，重新创建一个项目不仅仅需要把代码粘贴过去，还需要把大量的元件原封不动地粘贴过去，我试过，很难成功。

因此这个项目的代码仅作为参考使用，该版本的SWF文件可以使用，没有问题。

建议在增加陈老师所说的界面之后，如果可以的话，直接利用该SWF文件，而不是重新编写，当然，实在需要的话，重新编写也是可以的。可以参考该项目代码。

说明：
block.fla中，只有场景的第一帧帧上的代码、元件sprite 273（即settingsWindow)的代码、元件sprite 54（即block组件）的代码是我们项目需要的逻辑代码，其余的元件上的代码均是flash组件和导入的类的代码，不用去管。