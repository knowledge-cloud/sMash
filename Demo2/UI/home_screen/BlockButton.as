class BlockButton
{
    var blockButton;
    function BlockButton()
    {
        blockButton.focus._visible = false;
    } // End of the function
    function loadIcon(url)
    {
        blockButton.blockIcon.loadMovie(url);
    } // End of the function
} // End of Class
