把blocks文件夹放入D:\Pub\htdocs\mashup\~tmp并覆盖原有的blocks文件夹
把block.swf放入D:\Pub\htdocs\mashup\~tmp并覆盖原有的block.swf文件
把mobile_mashup_recommendation_cgi.exe和blocks.xml文件放入D:\Pub\apache2\cgi-bin下,如有原文件则覆盖

浏览器中输入http://localhost:8888测试运行

推荐列表在点击界面中的block之后出现在设置panel里，点击推荐列表中的block也可以加到界面中去

注：目前因为刘颖他们对block的描述和卢宾师兄的不同，故推荐是按照卢宾的block描述进行参数匹配，结果也是来自于卢宾之前定义的block（这两天会将现有的block进行类比描述，从而将两面的工作结合起来），而实际mashup的运行则还是刘颖他们之前做的。